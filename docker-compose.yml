version: "3.1"
services:
  web:
    build: .
    ports:
      - 4000:4000
    volumes:
      - .:/srv/jekyll
    command: jekyll build
  staging:
    build: _drafts/staging
    ports:
      - 8080:80
    volumes:
      - ./_site:/var/www/html

