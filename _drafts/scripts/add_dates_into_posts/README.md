# Add dates into posts

This will retrospectively add dates to all posts in the _posts folder so they are compatible with netlify.

Install using requirements.txt

```bash
pipx install -r requirements.txt
```
